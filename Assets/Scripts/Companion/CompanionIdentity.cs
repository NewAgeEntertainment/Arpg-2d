using UnityEngine;

/// Attach to any companion scene object that can be recruited.
/// The id must match the roster id you pass to PartyRecruit("id").
public class CompanionIdentity : MonoBehaviour
{
    public string id;
}
